# CSS Parser

This library assumes that the CSS complies with valid CSS w3 [declarations](https://www.w3.org/TR/CSS22/syndata.html#declaration)

## How to run

```bash
zig build run
```

## How to test

```bash
zig build test --summary all
```

The output will be in the `output.json` file
